import { Section } from "@/app/components";

export const Projects = () => {
    return (
        <Section classes="bg-blue-300">
            <h2 className="text-6xl font-bold">Projects</h2>
        </Section>
    );
};
