import { Section } from "@/app/components";

export const Contact = () => {
    return (
        <Section classes="bg-red-300">
            <h2 className="text-6xl font-bold">Contact</h2>
        </Section>
    );
};